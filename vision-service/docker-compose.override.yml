services:
  vision-service:
    build: .
    env_file:
      - .env
    ports:
      - "${SERVICE_PORT:-8001}:8001"
    volumes:
      - ./cache:/app/cache
      - ./weights:/app/weights
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
